# Example backend environment variables (do NOT commit your real .env)
# Copy this file to `.env` or `.env.local` in your backend folder and set real secrets.

# Database connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ludotec_db
DB_USER=postgres
# Set your DB password here. Example provided from user input (store securely, do not commit):
DB_PASSWORD=aL955TEC4598

# Optional consolidated connection string (can be generated programmatically):
# DATABASE_URL=postgres://postgres:yourpassword@localhost:5432/ludotec_db
